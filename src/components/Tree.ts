import { defineComponent,h } from 'vue';

export default defineComponent({
    name: 'Tree',
    props: {},
    setup(props) {
    },
    render() {
      return h(
        'div','aaaa'
      )
    },
})